<UserControl x:Class="vtb_fitness_client.UserControls.TariffPriceUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:vtb_fitness_client.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="50"
             d:DesignWidth="100">
    <UserControl.Resources>
        <Style TargetType="TextBlock"
               BasedOn="{StaticResource global_TextBlock}">
            <Setter Property="VerticalAlignment"
                    Value="Center" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="price_TextBlock"
                   Text="66666"/>
        <TextBlock x:Name="discountedPrice_TextBlock"
                   Text="6666"
                   Grid.Column="1"/>
    </Grid>
</UserControl>
